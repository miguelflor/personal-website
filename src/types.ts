export enum Side {
  Left = "left",
  Right = "right",
  Front = "front",
  Back = "back",
}

// Where the model's local origin sits along the z axis.
// x always centers; y always anchors to the bottom.
export enum ZAnchor {
  Center = "center",
  Back = "back",
  Front = "front",
}
